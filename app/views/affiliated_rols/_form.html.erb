<%= form_with(model: affiliated_rol, local: true) do |form| %>
  <% if affiliated_rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(affiliated_rol.errors.count, "error") %> prohibited this affiliated_rol from being saved:</h2>

      <ul>
      <% affiliated_rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
